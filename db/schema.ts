// Intentionally empty — no product tables yet (per CLAUDE.md: no schema
// scaffolding ahead of a real ticket). This file exists purely so
// drizzle-kit has a valid schema entrypoint to point at.
export {};
